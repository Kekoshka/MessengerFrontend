<Page x:Class="MessengerWebAPIFrontend.Pages.Main.Main"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MessengerWebAPIFrontend.Pages.Main"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Main">

    <Grid Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid Height="50" VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Background="Transparent" BorderThickness="0">
                        <Image Source="/Images/burgerMenu.png"/>
                    </Button>
                    <TextBox  x:Name="SearchChats" Grid.Column="1" Margin="5"/>
                </Grid>
                
                <StackPanel x:Name="ChatsParent" Margin="0,50,0,0">
                    
                </StackPanel>
            </Grid>
            <Frame Grid.Column="1" x:Name="ChatParent" NavigationUIVisibility="Hidden"/>

        </Grid>
    </Grid>
</Page>
